<snippet>
	<content><![CDATA[
# =========== 遍历 ls 输出内容
    # ls xxxxx | while read line;do echo $line ;done

# ===========  倒序遍历数组
    # i=\${#hashLIst[@]}
    # while [ \$i -gt "0" ]
    # do
    #   let i--
    #   #\${hashLIst[\$i]}
    # done

# ===========  字符串分割成数组
    #bnList=\$(echo $branchName|tr "_" " ")

]]></content>
	<tabTrigger>xar</tabTrigger>
	<scope>source.shell</scope>
	<description>特殊数组demo</description>
</snippet>
